[project]
name = "email-watch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "o365",
    "slack-sdk>=3.41.0",
]

[tool.uv.extra-build-dependencies]
cryptography = []
